package httpHelpers

import (
	"encoding/json"
	"fmt"
	"net/http"
)

func RespondWithJson(x) string {
	byteArray, err := json.Marshal(x)
	if err != nil {
		fmt.Println(err)
	}
	return string(byteArray)
}
